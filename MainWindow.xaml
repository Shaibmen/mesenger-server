<Window x:Class="Server_app.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Server_app"
        mc:Ignorable="d"
        Title="MainWindow" Height="300" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
          
        </Grid.ColumnDefinitions>
        <Label Grid.Row="0" Content="Добро пожаловать в сетевой чат!" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" TextAlignment="Left" VerticalAlignment="Bottom" Text="Введите своё имя пользователя"/>
        <Button Grid.Row="2" Content="Я хочу создать новый чат" VerticalAlignment="Center" Click="Create_Click"/>
        <Button Grid.Row="3" Content="Я хочу войти в уже существующий чат" VerticalAlignment="Center" Click="Join_Click"/>
        <TextBox Grid.Row="4" TextAlignment="Left" VerticalAlignment="Bottom" Text="Введите IP адресс-чата"/>
    </Grid>
</Window>
